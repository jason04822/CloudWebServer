<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library Management System - Login</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-image: url('/images/toy_story-wallpaper-1600x900.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
        }
        .login-container {
            background-color: rgba(255, 255, 255, 0.008);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 6px 6px rgba(0, 0, 0, 0.5);
            width: 300px;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        .login-container h2 {
            margin-bottom: 20px;
            color: #faf6f6;
        }
        .login-container input {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            background-color: rgba(255, 255, 255, 0.9);
            color: #848282;
        }
        .login-container input::placeholder {
            color: #848282;
        }
        .login-container button {
            width: 30%;
            padding: 8px 16px;           
            margin-top: 10px;
            background: #85dafe;
            color:#ffffff;
            border: none;
            border-radius: 60px;       
            font-weight: bold;
            font-size: 0.9rem;
        
            cursor: pointer;
            transition: all 0.3s ease;
        }
        .login-container button:hover {
            background-color: #40c8f5;
            color:#ffffff
        }
        .error {
            color: #ff4444;
            margin-top: 10px;
        }
        .login-container a {
            color: #848282; 
            text-decoration: none;
        }
        .login-container a:hover {
            text-decoration: underline;
        }
        /* Fallback for browsers without backdrop-filter support */
        @supports not (backdrop-filter: blur(10px)) {
            .login-container {
                background-color: rgba(255, 255, 255, 0.5); 
            }
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h2>Welcome Back</h2>
        <% if (messages && messages.error) { %>
            <p class="error"><%= messages.error %></p>
        <% } %>
        <form method="POST" action="/login">
            <input type="text" name="email" placeholder="Email" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit">Login</button>
        </form>
        <p style="margin-top:18px; color: #575555; font-size: 0.95rem;">
            Don't have an account? 
            <a href="/signup" style="color: #575555; text-decoration: underline;">Sign up</a>
        </p>
    </div>
</body>
</html>
